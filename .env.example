# Local only — not committed. Copy to .env and fill.

# Read production API (or http://127.0.0.1:8000 for local)
READ_API_URL=https://read-api.antunai.com

# Same value as HUB_SYNC_TOKEN on the Read VM
READ_HUB_TOKEN=

# Optional: lock the curator UI
# KNOWLEDGEHUB_OPS_SECRET=

# Translation desk (Grotius pilot)
DEEPSEEK_API_KEY=
GEMINI_API_KEY=
