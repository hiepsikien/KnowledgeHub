Apply on hiepsikien/Read, branch cursor/ref-pilot-ingest-render-adc9
(the branch that already has tokensFromRefSpans). From the Read repo root:

  git checkout cursor/ref-pilot-ingest-render-adc9
  git apply read-span-note-footnotes.patch

diff --git a/packages/api-client/src/index.ts b/packages/api-client/src/index.ts
index edbab33..0b2685f 100644
--- a/packages/api-client/src/index.ts
+++ b/packages/api-client/src/index.ts
@@ -990,6 +990,49 @@ function matchFootnoteNote(marker: string, notes: ReaderNote[]): ReaderNote | un
   return undefined;
 }
 
+function spanNoteId(marker: string): string {
+  return `span-note:${marker || "note"}`;
+}
+
+/**
+ * Merge Hub ``span.note`` bodies into the notes list used for matching/display.
+ * Prefer a non-empty ``notes[]`` summary; otherwise take ``span.note``.
+ * Web/mobile clients that pass ``data.notes`` separately should call this
+ * before ``tokensFromRefSpans`` / ``uniqueNotesFromTokens``.
+ */
+export function notesFromRefBlocks(
+  refBlocks: RefBlock[] | undefined | null,
+  notes: ReaderNote[]
+): ReaderNote[] {
+  const merged: ReaderNote[] = notes.map((note) => ({
+    ...note,
+    aliases: [...(note.aliases || [])],
+  }));
+  for (const block of refBlocks || []) {
+    for (const span of block.spans || []) {
+      if (String(span.style || "") !== "footnote") continue;
+      const body = String(span.note || "").trim();
+      if (!body) continue;
+      const marker = String(span.text || "").trim();
+      const existing = matchFootnoteNote(marker, merged);
+      if (existing) {
+        if (!(existing.summary || "").trim()) existing.summary = body;
+        continue;
+      }
+      merged.push({
+        id: spanNoteId(marker),
+        name: marker || "[note]",
+        aliases: marker ? [marker] : [],
+        episode_key: "",
+        episode_title: "",
+        group_label: "Chú thích",
+        summary: body,
+      });
+    }
+  }
+  return merged;
+}
+
 function mergeAdjacentTokens(tokens: InlineMarkdownToken[]): InlineMarkdownToken[] {
   const out: InlineMarkdownToken[] = [];
   for (const token of tokens) {
@@ -1059,6 +1102,7 @@ export function tokensFromRefSpans(
       const marker = String(span.text || chunk).trim();
       const note = matchFootnoteNote(marker, notes);
       if (note) noteId = note.id;
+      else if (String(span.note || "").trim()) noteId = spanNoteId(marker);
     }
 
     tokens.push({ text: chunk, bold: false, italic, noteId });
@@ -1102,8 +1146,8 @@ export function buildReaderBlocks(
   content: string,
   options?: { refBlocks?: RefBlock[] | null; notes?: ReaderNote[] }
 ): ReaderRenderBlock[] {
-  const notes = options?.notes ?? [];
   const refBlocks = options?.refBlocks;
+  const notes = notesFromRefBlocks(refBlocks, options?.notes ?? []);
   if (refBlocks && refBlocks.length) {
     const out: ReaderRenderBlock[] = [];
     for (const block of refBlocks) {
diff --git a/packages/api-client/src/inline-markdown.test.ts b/packages/api-client/src/inline-markdown.test.ts
index f84a526..db38160 100644
--- a/packages/api-client/src/inline-markdown.test.ts
+++ b/packages/api-client/src/inline-markdown.test.ts
@@ -139,7 +139,9 @@ assert.deepEqual(annotateInlineTokens(thesis, []), parseInlineMarkdown(thesis));
 
 import {
   buildReaderBlocks,
+  notesFromRefBlocks,
   tokensFromRefSpans,
+  uniqueNotesFromTokens,
 } from "./index.ts";
 
 const arnoldNote: ReaderNote = {
@@ -198,4 +200,41 @@ if (rendered[0]?.kind === "prose") {
   assert.ok(rendered[0].tokens.some((token) => token.noteId === "note-1"));
 }
 
+const hubSpan = {
+  style: "footnote",
+  start: 32,
+  end: 35,
+  text: "[1]",
+  note: "Hub body",
+} as const;
+
+const spanOnly = tokensFromRefSpans(
+  "Poetry is the criticism of life.[1]",
+  [hubSpan],
+  []
+);
+assert.ok(spanOnly.some((token) => token.noteId && token.text === "[1]"));
+
+const stale: ReaderNote = {
+  id: "stale-1",
+  name: "[1]",
+  aliases: ["[1]"],
+  episode_key: "ch-001",
+  episode_title: "",
+  group_label: "Chú thích",
+  summary: "",
+};
+const merged = notesFromRefBlocks(
+  [{ type: "paragraph", text: "Poetry is the criticism of life.[1]", spans: [hubSpan] }],
+  [stale]
+);
+assert.equal(merged.find((note) => note.id === "stale-1")?.summary, "Hub body");
+const staleTokens = tokensFromRefSpans(
+  "Poetry is the criticism of life.[1]",
+  [hubSpan],
+  merged
+);
+assert.ok(staleTokens.some((token) => token.noteId === "stale-1"));
+assert.equal(uniqueNotesFromTokens(staleTokens, merged)[0]?.summary, "Hub body");
+
 console.log("ok");
